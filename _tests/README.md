# README

This package is all the unit and component tests for `package:kelicap`. They are maintained here in order to avoid declaring circular dependencies between`kelicap` and `kelicap_test`, as well as adding private testing infrastructure.
