name: kelicap_test
description: >
  Testing runner and library for Kelicap.
  This package is necessary to write component tests for Kelicap components.
version: 1.1.0
repository: https://github.com/dukefirehawk/kelicap

environment:
  sdk: ">=3.13.0 <4.0.0"

resolution: workspace

dependencies:
  collection: ^1.17.0
  meta: ^1.8.0
  kelicap: ^1.0.0
  web: ^1.1.1

dev_dependencies:
  build_runner: ^2.14.0
  build_test: ^3.0.0
  build_web_compilers: ^4.6.0
  lints: ^6.0.0
  test: ^1.31.0
