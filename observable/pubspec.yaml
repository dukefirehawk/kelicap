name: kelicap_observable
version: 1.1.0
description: >
  Observable components for Kelicap framework.
  Provides support for marking objects as observable
repository: https://github.com/dukefirehawk/kelicap

environment:
  sdk: '>=3.13.0 <4.0.0'

resolution: workspace

dependencies:
  collection: ^1.11.0
  meta: ^1.3.0
  quiver: ^3.0.1

dev_dependencies:
  build_runner: ^2.1.2
  build_test: ^3.5.0
  build_web_compilers: ^4.8.0
  lints: ^6.0.0
  test: ^1.20.4
